#include "instructions.h"

int ADD.Imm.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int ADD.Imm.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int ADD.Imm.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int ADD.Imm.T4(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int ADD.Reg.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int ADD.Reg.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int ADD.Reg.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int B.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int B.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int B.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int B.T4(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int BL.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int BX.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int CMP.Imm.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int CMP.Imm.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int CMP.Reg.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int CMP.Reg.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int CMP.Reg.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int IT.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int LDR.Imm.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int LDR.Imm.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int LDR.Imm.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int LDR.Imm.T4(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int LDR.Lit.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int LDR.Lit.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int MOV.Imm.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int MOV.Imm.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int MOV.Imm.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int MOV.Reg.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int MOV.Reg.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int MOV.Reg.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int MOVT.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int MUL.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int MUL.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int POP.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int POP.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int POP.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int PUSH.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int PUSH.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int PUSH.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int STR.Imm.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int STR.Imm.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int STR.Imm.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int STR.Imm.T4(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int STR.Reg.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int STR.Reg.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int SUB.Imm.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int SUB.Imm.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int SUB.Imm.T3(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int SUB.Imm.T4(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int SUB.Reg.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int SUB.Reg.T2(word binary, memory mem, int* in_it, int* it_state) {return 0;}
int SVC.T1(word binary, memory mem, int* in_it, int* it_state) {return 0;}
